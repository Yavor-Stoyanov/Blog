<header>
    <nav>
        <ul>
            <% headerLinks.forEach(link => { %>
                <li><a href="<%= link.url %>"><%= link.text %></a></li>
            <% }); %>
        </ul>
    </nav>
</header>