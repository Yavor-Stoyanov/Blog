<aside>
  <div class="welcome-container"> 
    <h2 id="welcome-message">Welcome to Trip Chaser ✈</h2>
  </div>

  <% if (locals.weather) { %>
    <div class="weather-widget">
      <div class="weather-content">
        <h2 class="city-name"><%= weather.name %></h2>
        <p class="temperature"> <%= Math.round(weather.main.temp) %>°C</p>
        <img src="https://openweathermap.org/img/wn/<%= weather.weather[0].icon %>.png" alt="Изображение на времето" class="weather-icon" />
      </div>
    </div>
  <% } %>
</aside>
